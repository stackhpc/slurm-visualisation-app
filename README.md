An app plugin for grafana to help visualise slurm clusters

Build instructions
```
place app in grafana plugin directory
yum install npm
npm install -g grunt
cd into plugin root directory (ie. where package.json is found)
npm install
grunt
```
